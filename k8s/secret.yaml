# Secret template — valores reales se aplican manualmente o via sealed-secrets.
# NO commitear valores reales.
apiVersion: v1
kind: Secret
metadata:
  name: akopia-secrets
  namespace: akopia
type: Opaque
stringData:
  BEARER_TOKEN: "CHANGE_ME"
  GITEA_TOKEN: "CHANGE_ME"
  MEILI_MASTER_KEY: "CHANGE_ME"
  QDRANT_API_KEY: ""
